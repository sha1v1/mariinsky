node_modules
uploads/*
!uploads/.gitkeep
data/orbs/*.json
.DS_Store

# the page's objects are generated from the asset library by `npm run items`,
# not checked in — 44 photographic cut-outs is a few megabytes of derived
# artefact, and the originals live outside the repo
public/images/items/
