node_modules
uploads/*
!uploads/.gitkeep
data/orbs/*.json
.DS_Store

# holds the fal.ai key for semantic layer decomposition. .env.example is the
# committed one, and it is empty.
.env
*.local
/dist/
