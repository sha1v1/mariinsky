# Optional. Only needed for semantic (Qwen) layer decomposition in the lab.
# Get one at https://fal.ai/dashboard/keys — it is pay-per-use.
# Everything else in this app runs with no key and no network.
FAL_KEY=

# Overridable if fal moves the endpoint or you want the 640px-class variant.
# FAL_MODEL=fal-ai/qwen-image-layered
# FAL_BASE=https://queue.fal.run
