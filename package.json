{
  "name": "mariinsky",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "a memory collective — an i-spy wall of memories that come back different, and fade, every time they are looked at",
  "workspaces": [
    "world"
  ],
  "scripts": {
    "build": "npm run build --workspace memory-world",
    "start": "npm run build && NODE_ENV=production node --env-file-if-exists=.env --env-file-if-exists=world/.env.local server.js",
    "dev": "NODE_ENV=development node --env-file-if-exists=.env --env-file-if-exists=world/.env.local server.js",
    "seed": "node scripts/seed.js"
  },
  "engines": { "node": ">=22.18" },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
