{
  "name": "mariisnky",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "i-spy memory garden — a shared wall of memories that fade as they are looked at",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node scripts/seed.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
